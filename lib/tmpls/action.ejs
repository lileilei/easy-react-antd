/**
 * <%=funcName%>方法
 */
export const <%=name.toUpperCase()%> = '<%=name.toUpperCase()%>'
export function <%=funcName%>Type(result) {
    return {
        type: <%=name.toUpperCase()%>,
        result: result
    }
}
export function <%=funcName%>(opts, type) {
    return (dispatch, getState) => {
        var interFace = '/api/temp/getList';
        return axios.post(interFace, opts)
            .then(function (result) {
                dispatch(<%=funcName%>Type(result.data))
            }).catch(function (error) {
                console.error("URL: " + interFace + error)
            })
    }
}
/*@action*/