import Home from './containers/HomeContainer'

import Redirect from '../PageNotFound/redirect'
export default (store) => ({
  path: 'home',
  component: Home,
  indexRoute: {
    onEnter (nextState, replace) {
      replace('/<%=project%>/home/view')
    }
  },
  childRoutes: [

    Redirect
  ]
})

